<!--index.wxml-->
<view class="header">
  <view class="h1-super-header">FML</view>
  <view class="h1-header">Your Daily F**k Stories</view>
</view>

<view wx:if="{{stories.lenght == 0}}">No stories</view>
<view wx:else>
  <view wx:for="{{stories}}">
    {{index}}: {{item.story}}
  </view>


</view>


